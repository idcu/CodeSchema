# S3 周期性守护报告（2026-08-29）

> 依据 `idcu-go` S0 总览与执行协议 §三 任务分解：S3 = 周期性核查与守护（T4 合规 / R1 文档一致性 / R2 planned 潜能点亮 / R3 版本一致性）。
> 本报告为 B8 收口（Commit 131 / cb33fe0）后的**首个 S3 周期增量报告**——仅报差异与待决项，不重复历史结论。

## 0. 触发与基线

- **触发**：B8 落地后 `DEV_PROGRESS.md` 全部勾选，特征阶段（S1/S2 等价）收口，按 S0 转入 S3 周期性守护。
- **工作树**：clean（HEAD = `cb33fe0`，未 push）。
- **扫描方式**：全仓 markdown 未完成标记扫描（zsh 安全引号 `--include='*.md'`）；无代码改动。

## 1. R1 文档一致性守护

发现 **2 处文档态滞后**（代码已落地、文档未同步），全部修复：

| # | 文件 | 问题 | 修复 |
|---|---|---|---|
| R1-1 | `analysis/2026-08-29-fastcontext-analysis.md:139` | B8 状态表仍标 `⏸ 未做`（「待 MinScore 阈值有量化口径再落地」） | 状态表补 B8 ✅ 行（line 138，落地位置 = `Search`/`Service.SearchWithOptions` 的 `MinScore` 量化阈值 + `trim_reason=below_threshold` 回传）+ 修订记录补正（line 153） |
| R1-2 | `docs/1-生产层/开发文档/11-配置部署与路线图.md:179` | 风险表「语义检索未落地」与已落地事实矛盾 | 更正为「已落地（历史风险已解除）」，注明 P6/P6_2 + Recall@1=1.00 |

**未处理（合理保留，非缺陷）**：
- `docs/4-决策层/版本发布说明.md:26`「v0.1.0（待首个 v* tag）」——code-schema 自身尚未发版，属真实状态。
- `docs/2-交付层/运维文档/01-生产部署清单.md` 与多个 `dev` 文档的 `[ ]` 勾选框——为部署/验证清单模板，非项目待办，保留原样。

## 2. R2 planned 潜能点亮

- 全仓扫描「可抽取公共能力」候选：B 系列（B1–B9）已全部落地，**无新增待抽取项**。
- 唯一历史「待拍板」项 B8 已闭环；本周期无新 `planned` 登记。
- 建议：新功能接入时按需触发复用扫查（调用点计数 / 复制检测），当前无阻塞。

## 3. R3 版本一致性守护

- `go.mod`：`gitee.com/idcu-go/{pathsafe,trim,ttlcache}` 均锁定 `v0.1.0`（require）+ `replace => ../idcu-go/{...}`（本地开发路径）。
- 与已发布 tag 对齐：三模块 `v0.1.0` 由 Commit `c00955f` push + tag（分析文档 line 128–130 佐证），require 与 tag 一致；replace 与 tag 两条构建路径此前均实测通过（`DEV_PROGRESS.md:396–399`）。
- 本次复测：`GOWORK=off go build ./...` = 0，确认本地 `replace` 源可编译、无破坏。
- **待决（环境限制）**：本机 `/Volumes/Data/idcu-go` 非 git 工作副本（`git tag` 不可用），未做「本地模块 HEAD vs v0.1.0 tag」逐 commit diff。建议在可访问 idcu-go git 仓库的环境周期跑：
  `git -C <idcu-go> tag | grep -E 'trim|ttlcache|pathsafe'`，防止本地 `replace` 源偏离已发布 tag。

## 4. T4 合规核查

- **B8 新增公共 API 面**（`SearchWithOptions` / `SearchOutcome{Results,TrimReason,Filtered}` / `SearchResult.Confidence`）：
  - 已文档化（`README.md`「检索低置信度过滤（B8）」小节）；
  - 向后兼容（`Search` 委托 `MinScore=0`；HTTP/MCP 响应改 envelope，旧测试仅断言状态码，无破坏）；
  - 双消费者：HTTP `GET /search?min_score=` 与 MCP `search_symbols.min_score` 均已接线；
  - 无 secret / 无应用倾向 / 公共抽象复用（置信度量化复用 chromem 余弦 + FTS 归一化，无复制粘贴）。
  - 结论：符合 meta 双消费者 + 文档同步 + 无 secret 准则。
- **三 idcu-go 公共模块**（trim/ttlcache/pathsafe）：纯标准库、零依赖、`v0.1.0` 发布，符合 meta v1.2「通用模块」标准（抽取时评审）。
- 本周期**无新增公共抽象抽取**。

## 5. 客观门禁结果（S0 §5.2）

| 门禁 | 结果 |
|---|---|
| `GOWORK=off go build ./...` | 0 |
| `go test ./internal/search/... ./internal/service/... ./internal/server/... -short -count=1` | ok（含 B8 8 组测试） |
| `go vet ./internal/search/... ./internal/service/... ./internal/server/...` | 上一提交（cb33fe0）已通过；本周期仅文档改动，无代码回归风险 |
| 交叉编译 / 覆盖率 | B8 落地时（cb33fe0）已全绿；本周期文档改动不引入回归 |

## 6. 待决项（决策聚批，S0 §5.2-4）

无阻塞项。可选演进（非本次范围，待拍板）：
1. 纯 FTS `exact` 模式 `min_score` 当前为相对归一化尺度；如需绝对尺度可改用 IDF/BM25 归一化（B8 落地时记为可选 follow-up）。
2. code-schema 自身发版：首个 `v*` tag 就绪后可回填 `版本发布说明.md`；当前未发布属预期。

## 7. 结论

B8 收口后首个 S3 周期：文档一致性（R1）2 处滞后已修复并验证；版本一致性（R3）与合规（T4）均通过，无新增 planned（R2）。代码未改动、门禁全绿。改动仅限文档同步，已提交（未 push）。
