# Binaries
/codeschema
/codeschema.exe
*.exe
*.dll
*.so
*.dylib

# Go
*.test
*.out
go.work

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build artifacts
/bin/
/dist/
build/codeschema
build/codeschema.exe

# Multi-tenant demo runtime (generated by build/mt-demo.yaml; keep mt-demo.yaml itself)
build/mt-a/
build/mt-b/
build/mt-*.log

# Data files
*.db
*.db-wal
*.db-shm
vector.db
scipout/

# Config
.env
config.local.yaml

# Dependencies
node_modules/
vendor/

# Dependency archives (not needed for build)
down/*.zip
down/*.tar.gz
down/*.tgz
down/*.gz
down/go.mod
down/go.sum

# ONNX model files (large binary, download separately)
down/models/

# Temporary analysis docs (competitor analyses, reviews, drafts — not deliverables)
/analysis/

# Analysis documents under docs/ (not version-controlled)
/docs/analysis/

# Published articles / external drafts (not version-controlled)
/articles/

# Agent workspace memory (private, per-machine)
.workbuddy/

# Runtime storage output (FileStore JSON driver writes here)
/data/

# LSP adapter real-server E2E test artifact
/cs_lsp_tmp/

# Build/runtime bench outputs (regenerated; *.exe/*.dll already ignored above)
build/*.json
build/*.jsonl
build/*.db
build/*.log
build/*.html
build/models-*.tar.gz
build/models-*.sha256
build_err.txt

# Adapter benchmark 测试的历史残留（已被仓库根 build/analysis 取代；可能被本机杀软锁定）
/internal/adapterbench/build/
/internal/adapterbench/analysis/

# 已被迁移到 internal/adapterbench 的旧副本残留（平台安全删除守卫拦截无法物理删除，
# 已加 build-ignore 不参与编译，此处忽略使其不污染 git 状态）
/internal/integration/adapter_validation_test.go

# Multi-tenant build output (generated artifact per module, not source)
build/idcu-mt/

# ONNX inference binary (large Mach-O executable, build artifact — must not be tracked)
/codeschema-onnx
